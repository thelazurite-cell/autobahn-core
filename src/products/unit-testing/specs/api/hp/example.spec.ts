import { expect } from 'chai';

describe('First Example API testing file', () => {
    it('should demonstrate that the functionality works as expected', () => {
        expect(true).to.eql(true);
    });
});