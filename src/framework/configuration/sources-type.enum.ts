export enum SourcesType {
    /**
     * Used for browser (testcafe) tests
     */
    browser = 'browser',
    /**
     * Used for API (Mocha) tests
     */
    api = 'api',
    /**
     * used for API (Postman/Newman) tests
     */
    newman = 'newman'
}
