/***
 * Determines how page elements using be searched for on a page.
 */
export enum LocatorType {
    Fallback,
    Id,
    Css,
    AllCss,
    Link,
    Button,
    Frame,
    XPath,
    AllXPath,
    Name
}
